name: golden_toolkit
description: Common patterns & APIs for improved testing with Goldens
version: 0.0.1
author:
homepage: https://github.com/eBay/flutter_glove_box/tree/master/packages/golden_toolkit

environment:
  sdk: ">=2.6.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  meta: ^1.1.8
  flutter_test:
    sdk: flutter

dev_dependencies:
  pedantic: ^1.8.0

# needed so the images can be loaded in the example tests
flutter:
  assets:
    - images/
